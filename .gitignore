.git
.idea
node_modules
nbproject
admin/i18n/flat.txt
admin/i18n/*/flat.txt

# ioBroker dev-server
.dev-server/
.vscode/launch.json

#ignore .commitinfo created by ioBroker release script
.commitinfo

# NYC coverage files
coverage

# TypeScript build output is committed for npm installs without a build step
# (standard for many community adapters); local rebuild via `npm run build`
